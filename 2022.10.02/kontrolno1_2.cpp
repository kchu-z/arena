#include <bits/stdc++.h>
using namespace std;

int main() {

    long long n;
    cin >> n;
    vector <long long> v;

    for (int i = 2; i * i <= n; i++) {
        if (n % i != 0) continue;
        if (n != i * i)
    }

    return 0;

}
