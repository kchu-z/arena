#include <bits/stdc++.h>
using namespace std;

int main() {

    vector <int> v(4), s(4);
    cin >> v[0] >> v[1] >> v[2] >> v[3];
    sort(v.begin(), v.end());

    for (int i = 0; i < 4; i++) {
        for (int i < n; i++)
    }

    do {

    } while (next_permutation(s.begin(), s.end()));

    return 0;

}
