#include <bits/stdc++.h>
using namespace std;

long long a[501][125001]

long long f(int n, int k) {
    if (k < 0) return 0;

    if (n == 0) {

    }
}

int main() {

    int n, k;
    cin >> n >> k;

    cout << f(n, k);
    return 0;

}
