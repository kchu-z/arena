#include <bits/stdc++.h>
using namespace std;

int n, m;
vector <pair <int, int>> edges[400005];

int depth[400005];
int

int main() {

    ios_base::sync_with_stdio(false);
    cin.tie(0);

    int n, m;
    cin >> n >> m;

    for (int i = 1; i <= m; i++) {
        int x, y, w;
        cin >> x >> y >> w;

    }

    queue <int> q;
    q.push

    visited[1] = true;

    return 0;

}
