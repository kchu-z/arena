#include <bits/stdc++.h>
using namespace std;

int main() {

    unsigned long long k, n, x = 0, y = 0;
    cin >> k >> n;

    unsigned long long limit = pow(2, n);

    for (unsigned long long i = 1;)

    cout << x << ' ' << y;
    return 0;

}
