#include <bits/stdc++.h>
using namespace std;

long long a[1000];

int main() {

    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int n;
    cin >> n;

    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }

    for (int i = 0; i < q; i++) {
        int x;
        cin >> x;


    }

    return 0;

}
